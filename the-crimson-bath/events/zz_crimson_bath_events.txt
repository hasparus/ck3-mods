namespace = crimson_bath

# The Crimson Bath — confirmation window for the rite.
crimson_bath.0001 = {
	type = character_event
	title = crimson_bath_decision
	desc = crimson_bath_decision_desc
	theme = dread
	override_background = { reference = dungeon }

	left_portrait = {
		character = root
		animation = personality_callous
	}
	right_portrait = {
		trigger = { exists = scope:crimson_bath_offering }
		character = scope:crimson_bath_offering
		animation = shame
	}

	immediate = {
		random_prisoner = {
			limit = { age < 30 }
			save_scope_as = crimson_bath_offering
		}
	}

	option = {
		name = crimson_bath_decision_confirm

		if = {
			limit = {
				has_trait = infirm
				any_prisoner = { count >= 1 age < 30 }
			}
			random_prisoner = {
				limit = { age < 30 }
				every_close_family_member = {
					add_opinion = {
						modifier = crimson_bath_kin_grief
						target = root
						years = 15
					}
				}
				death = {
					killer = root
					reason = death_sacrificed
				}
			}
			remove_trait = infirm
		}
		if = {
			limit = {
				has_trait = clouded_eyes
				any_prisoner = { count >= 1 age < 30 }
			}
			random_prisoner = {
				limit = { age < 30 }
				every_close_family_member = {
					add_opinion = {
						modifier = crimson_bath_kin_grief
						target = root
						years = 15
					}
				}
				death = {
					killer = root
					reason = death_sacrificed
				}
			}
			remove_trait = clouded_eyes
		}
		if = {
			limit = {
				has_trait = faltering_heart
				any_prisoner = { count >= 1 age < 30 }
			}
			random_prisoner = {
				limit = { age < 30 }
				every_close_family_member = {
					add_opinion = {
						modifier = crimson_bath_kin_grief
						target = root
						years = 15
					}
				}
				death = {
					killer = root
					reason = death_sacrificed
				}
			}
			remove_trait = faltering_heart
		}
		if = {
			limit = {
				has_trait = fragile_bones
				any_prisoner = { count >= 1 age < 30 }
			}
			random_prisoner = {
				limit = { age < 30 }
				every_close_family_member = {
					add_opinion = {
						modifier = crimson_bath_kin_grief
						target = root
						years = 15
					}
				}
				death = {
					killer = root
					reason = death_sacrificed
				}
			}
			remove_trait = fragile_bones
		}
		if = {
			limit = {
				has_trait = withering_mind
				any_prisoner = { count >= 1 age < 30 }
			}
			random_prisoner = {
				limit = { age < 30 }
				every_close_family_member = {
					add_opinion = {
						modifier = crimson_bath_kin_grief
						target = root
						years = 15
					}
				}
				death = {
					killer = root
					reason = death_sacrificed
				}
			}
			remove_trait = withering_mind
		}
		if = {
			limit = { NOT = { has_trait = witch } }
			give_witch_secret_or_trait_effect = yes
		}
		add_stress = minor_stress_gain
		if = {
			limit = { has_any_nickname = no }
			random_list = {
				50 = { give_nickname = nick_the_vampire }
				15 = { give_nickname = nick_the_red }
				15 = { give_nickname = nick_the_strigoi }
				10 = { give_nickname = nick_the_undying }
				10 = { give_nickname = nick_the_pale }
			}
		}
	}

	option = {
		name = crimson_bath.0001.b
	}
}
